# Pre-LSTM gauge summary (SportsMOT example, 45 frames)

Generated by `scripts/plot_pre_lstm_gauge.py`, `utils/metrics.py`, and `utils/visualize.py`.

## LSTM export gate

Source: `data/runs/sportsmot_example/trajectory_validation.json`

| Check | Result |
|-------|--------|
| **Passed** | **Yes** |
| Global visibility | **94.0%** (threshold ≥ 70%) |
| Frames with zero visible | **0** |
| Per-slot empty fraction | ≤ 26.7% (threshold ≤ 30%) |

**Verdict:** Tensors from `sanitize_plus_velocity_cap` are acceptable for LSTM v1 training.

---

## 1. SAM3 baseline (`baseline_metrics.png`)

- **Coverage:** 10.8 mean players/frame; 43/45 frames (95.6%) at full 10-player roster.
- **ID stability:** **0** ID switches; mean streak **32.3** frames.
- **Smoothness:** mean displacement **3.57 px**, max jump **15.01 px**.

**Verdict:** SAM3.1 baseline on this clip is already strong; augmentation is refinement, not rescue.

---

## 2. Augmentation (`summary_figure.png` + compare table)

Config: `sanitize_plus_velocity_cap` (LSTM v1 policy).

| Metric | Baseline | Augmented | Δ |
|--------|----------|-----------|---|
| Mean observed / frame | 10.76 | 10.42 | −0.33 |
| ID switches | 0 | 0 | 0 |
| Mean track streak | 32.27 | 27.93 | −4.33 |
| Mean displacement | 3.57 px | 3.67 px | +0.10 |
| Max jump | 15.01 | 15.01 | 0 |

**Verdict:** Sanitize trims ~15 crowd/over-detections; `velocity_cap` did not fire on this clip. Trade-off: slightly fewer boxes, marginally shorter streaks. Qualitative panels in `summary_figure.png`.

---

## 3. Real GT — ADE (`ablation_ade_bar.png`)

Source: `ablations/ablation_summary.csv` (aligned SportsMOT GT).

| Config | ADE (px) |
|--------|----------|
| **baseline** | **7.16** |
| dead_ball_freeze (best) | **7.08** |
| sanitize_plus_velocity_cap | 7.34 |
| full (all rules + gap-fill) | **20.36** |
| convergence_pull | 20.06 |
| cluster_cohesion | 17.18 |

**Verdict:** Minimal physical + sanitize is appropriate; **game-rule stack hurts badly** on real GT. Prefer `sanitize_plus_velocity_cap` or `dead_ball_freeze` for reporting; avoid `full`.

---

## 4. Robustness — multi-seed (`multi_seed_ade_bar.png`)

Source: `seeds/multi_seed_summary.json` — `sanitize_plus_velocity_cap` per seed, per-seed `gt_aligned.json`.

| Seed | ADE (px) |
|------|----------|
| offset_0s | 7.48 |
| offset_10s | 5.34 |
| offset_15s | 5.01 |
| **Mean ± std** | **5.94 ± 1.09** |

**Verdict:** Augmentation ADE is **stable across temporal windows** (10s/15s offsets slightly lower than 0s on this clip).

---

## Figure paths

All under `data/runs/sportsmot_example/figures/`:

- `baseline_metrics.png`
- `summary_figure.png`
- `ablation_ade_bar.png`
- `multi_seed_ade_bar.png`
- `compare_table.txt`

Regenerate:

```powershell
py utils/metrics.py --tracks data/runs/sportsmot_example/baseline_tracks.json --output-figure data/runs/sportsmot_example/figures/baseline_metrics.png --label "SAM3 Baseline"
py utils/visualize.py --frames data/runs/sportsmot_example/frames --baseline data/runs/sportsmot_example/baseline_tracks.json --augmented data/runs/sportsmot_example/ablations/sanitize_plus_velocity_cap/augmented_tracks.json --output data/runs/sportsmot_example/figures/summary_figure.png --summary --n-frames 4
py scripts/plot_pre_lstm_gauge.py --dataset sportsmot_example
```
